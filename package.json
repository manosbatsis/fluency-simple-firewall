{
  "name": "fluency_simple_firewall",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "ganache": "ganache-cli --accounts=15 --deterministic --defaultBalanceEther=1000000 --quiet"
  },
  "devDependencies": {
    "truffle": "^5.7.5"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^4.8.1"
  }
}
